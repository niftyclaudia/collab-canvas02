{
  "rules": {
    "sessions": {
      "main": {
        "users": {
          "$userId": {
            ".read": "auth != null",
            ".write": "auth != null && auth.uid == $userId",
            "cursor": {
              ".write": "auth != null && (auth.uid == $userId || !data.exists())"
            },
            "presence": {
              ".write": "auth != null && (auth.uid == $userId || !data.exists())"
            }
          }
        }
      }
    }
  }
}
